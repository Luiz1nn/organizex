services:
  organizex:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ..:/app
    environment:
      - PYTHONPATH=/app
    working_dir: /app
    entrypoint: ["python", "organizex.py"]
    command: []
